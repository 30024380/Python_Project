def main():
    x, y = 10, 100

    #If, elif, else statement that determines the result
    if (x < y):
        st = "x is less than y"
    elif (x == y):
        st = "x and y are the same"
    else: 
        st = "x is greater than y"
    print (st)

    #python does not have anything like switch statements
if __name__ == "__main__":
  main()
